<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Summary>C# Specification Pattern implementation.</Summary>
    <Description>
    A C# implementation of the Specification pattern that attempts to
    provide an ORM agnostic (though .NET specific) implementation which
    harnesses the abilities of the ORM to translate linq to the
    querying language of the underlying datastore.
    </Description>
    <TargetFramework>netstandard2.0</TargetFramework>
    <PackageId>Msq.Specification</PackageId>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Version>1.0.0</Version>
    <Authors>Morne Maritz</Authors>
    <RepositoryType>git</RepositoryType>
    <RepositoryUrl>https://github.com/mornemaritz/specification</RepositoryUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
  </PropertyGroup>

</Project>
